<div class="row">
 <h1>Super Zapatos</h1>
    <div class="large-12 columns">
      <div class="panel">
      <% if(notice) %>
      	<span class="round success label"><%= notice %></span>
      <% end %>
        <h2><%= @store.name %></h2>
		<fieldset>
			<legend>Address</legend>
			<%= @store.address %>
		</fieldset>
      </div>
      <a href="<%= edit_store_path(@store) %>" class="button">Edit</a>
	  <a href="/stores" class="button">Back</a>
    </div>
</div>  
